
global_plotting_options:
 regions: ["europe"]
 image_format : "png" 
 dpi_val : 300
 fps: 2
 ERA5:
   marker_size: 2
   scale_marker_size: 1
   marker: "o"

evaluation:
  metrics  : ["rmse","bias","mae"]
  regions: ["europe"]
  summary_plots : true
  ratio_plots : true
  heat_maps : true
  summary_dir: "./plots/MF_KNMI/eval_plots/"
  plot_ensemble: false #supported: false, "std", "minmax", "members"
  plot_score_maps: false #plot scores on a 2D maps. it slows down score computation 
  print_summary: false #print out score values on screen. it can be verbose
  log_scale: false
  add_grid: false
  score_cards: false
  bar_plots: true
  num_processes: "auto"  #options: int, "auto", 0 means no parallelism (default)

default_streams:
  CERRA:
    channels: ["2t", "10si", "10wdir", "r_850", "t_850", "u_850", "v_850", "z_500"] #, "blah"]
    evaluation:
      forecast_step: "all"
      sample: "all"
      ensemble: "all"
    plotting:
      sample: [0]
      forecast_step: [1,3,5,7] 
      ensemble: "mean"
      plot_maps: true
      plot_bias: true
      plot_target: true
      plot_histograms: true
      plot_animations: true
      
run_ids :
  xxx:
    label: